<%= form_for [question, answer, comment], :remote => true do |f| %>
  <%= f.text_area :body, :class => "input-xlarge" %> </br>
  <%= f.submit %>
  <%= link_to "Cancel", "#", :class => 'cancel-comment' %>
<% end %>
