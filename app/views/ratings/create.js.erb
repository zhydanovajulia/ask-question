$('.flash').html("<%=j render 'layouts/flash_messages', :flash => flash %>");
<% if @model.class.to_s == "Question" %>
	$('#<%= dom_id(@model) %>').text("<%= @model.ratings_count %>");	
<% elsif @model.class.to_s == "Answer" %>
	$('#<%= dom_id(@model) %>').text("<%= @model.ratings_count %>");	
<% end %>
	
